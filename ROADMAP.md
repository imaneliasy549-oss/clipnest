# نقشهٔ راه

این فایل کارهایی است که **هنوز انجام نشده‌اند**، به‌ترتیب اولویت، هر کدام با دلیل و
با این‌که چرا همین حالا انجام نشد. کارهای انجام‌شده در `CHANGELOG.md` هستند.

قاعده‌ای که همهٔ موارد زیر از آن پیروی می‌کنند: هیچ‌کدام نباید سازگاری پروتکل D-Bus را
بشکنند. دیمن، CLI و اکستنشن می‌توانند هر کدام جدا به‌روز شوند (کسانی هستند که اکستنشن
جدید را فقط بعد از logout/login می‌گیرند)، پس هر تغییر روی مرز، یا افزودنی است یا
version check دارد.

## اولویت بالا

### ۱. تنظیمات گرافیکی (`clipnest settings`)
پنجرهٔ GTK4/libadwaita برای همان کلیدهای `config.ini` فعلی: تعداد آیتم‌ها، سقف حجم
تصویر و متن، بودجهٔ کل تصاویر، مدت نگهداری، روشن/خاموش‌کردن پیست خودکار، تغییر
`paste_key` و میانبر.
**دلیل:** ویرایش فایل config برای کاربر عادی معقول نیست و هر کلیدی که فقط با فایل قابل
تغییر باشد عملاً پنهان است.
**چرا الان نه:** یک پنجرهٔ جدید + نوشتن فایل تنظیمات (با همان قواعد clamp و پیام‌های
خطا) یک تغییر مستقل و بزرگ است؛ باگ‌های فعلی اولویت داشتند.
**نکته:** باید همان فایل و همان schema را بخواند/بنویسد، نه schema دوم.

### ۲. انتخاب رفتار پیست
این‌که کاربر بگوید انتخاب یک آیتم یعنی فقط کپی، یا کپی + پیست، یا پیست فقط با Enter،
یا فقط با کلیک. امروز `pick` و `paste` دو دستور جدا هستند و پنل همیشه پیست می‌کند.
**دلیل:** بعضی کاربران پیست خودکار را روی همه‌چیز نمی‌خواهند و امروز راهی جز عوض‌کردن
`paste_key` (که کل قابلیت را خاموش می‌کند) ندارند.

### ۳. انتخاب منبع کلیپ‌بورد (`CLIPBOARD` و `PRIMARY`)
گرفتن `PRIMARY` هم (متن انتخاب‌شده) با تنظیم مستقل و بدون آیتم تکراری.
**دلیل:** روی لینوکس انتخاب‌کردن با ماوس عادت رایجی است.
**چرا الان نه:** به تغییر اکستنشن (`St.ClipboardType.PRIMARY`)، به dedup در دیمن و به
یک تصمیم دربارهٔ سیاست پیش‌فرض نیاز دارد؛ باید با تست همراه باشد.

### ۴. توقف گرفتن (privacy pause) و پاک‌سازی خودکار
`clipnest pause [minutes]` / `resume` و یک `clear_after_seconds` اختیاری.
**دلیل:** رمز و اطلاعات حساس هم به کلیپ‌بورد می‌روند و کاربر باید بتواند گرفتن را
موقتاً متوقف کند. این باید **کاملاً شفاف و opt-in** باشد و هیچ‌وقت داده را حدس نزند.

### ۵. فیلتر و دسته‌بندی
فیلتر بر اساس متن/تصویر/پین‌شده/امروز/این هفته و (اگر GNOME نام برنامهٔ مالک را
قابل‌اعتماد بدهد) بر اساس برنامه.
**دلیل:** با تاریخچهٔ ۵۰۰ آیتمی، جست‌وجوی متنی کافی نیست.

### ۶. پشتیبان‌گیری زمان‌بندی‌شده
`export` دوره‌ای با مسیر، تعداد نسخه‌های نگه‌داشته‌شده و نمایش فضای مصرفی.
**دلیل:** الان پشتیبان‌گیری دستی است و کسی که یادش برود، پشتیبان ندارد.

## اولویت متوسط

### ۷. جست‌وجوی بهتر
fuzzy search، نرمال‌سازی Unicode (حذف تفاوت `ی/ي` و `ک/ك`، اعراب) و برجسته‌کردن
بخش مطابق. **دلیل:** متن فارسی روی لینوکس با چند کدپوینت متفاوت تایپ می‌شود و امروز
جست‌وجوی «کتاب» ممکن است «كتاب» را پیدا نکند.

### ۸. پیش‌نمایش بهتر
بندانگشتی واقعی، MIME type، حجم، زمان کپی، تعداد دفعات استفاده.
**دلیل:** پنل امروز فقط نوع، ابعاد و پیش‌نمایش متنی را نشان می‌دهد؛ «کدام نسخه جدیدتر
بود» سؤال رایجی است که شمارش استفاده جواب می‌دهد (ستون `use_count` در
`clipboard_items` قدیمی بود و الان نگه داشته نمی‌شود).

### ۹. مدیریت آیتم‌های مشابه
ادغام آیتم‌های تکراری، شمارش استفاده، و انتخاب این‌که تکرار فقط زمان را بالا ببرد یا
ردیف جدید بسازد.

### ۱۰. دسترس‌پذیری و RTL
پیمایش کامل با صفحه‌کلید، برچسب‌های screen reader، مدیریت فوکوس، کنتراست، tooltip و
**چیدمان RTL برای فارسی** (پنل امروز LTR است).

### ۱۱. بومی‌سازی
ساختار ترجمه (فارسی، انگلیسی، عربی، آلمانی، فرانسوی) به‌جای متن‌های پراکنده در Rust و
JavaScript. CLI امروز فقط انگلیسی است و GitHub Copilot هم در بررسی‌اش به همین اشاره کرد.

## اولویت پایین‌تر

### ۱۲. رمزگذاری پشتیبان
`export` با رمز عبور (رمز ذخیره نمی‌شود، هشدار دربارهٔ فراموش‌شدنش، بدون فایل موقت
plaintext). **دلیل:** پشتیبان امروز متن خام است، از جمله چیزهایی که شاید رمز باشند.

### ۱۳. انتزاع backend
رابط `clipboard provider` و `paste provider` تا اگر روزی compositor دیگری لازم شد،
منطق UI با provider قاطی نشود. **دلیل:** امروز `app.rs` هم پنل است هم دیمن؛ برای یک
backend دوم باید جدا شوند — ولی تا وقتی backend دومی وجود ندارد، انتزاع زودهنگام است.

### ۱۴. بسته‌بندی بیشتر
Flatpak (که برای Portal و اجازه‌ها داستان جداگانه‌ای دارد)، AppStream metadata،
انتشار روی AUR/COPR، امضای artifactها، SBOM و بالابردن قابلیت بازتولید بیلد.
**دلیل:** امروز `.deb`، `.rpm`، `PKGBUILD` و تارْبال هست؛ اما چک‌سام SHA256 تنها چیزی
است که کسی می‌تواند با آن بررسی کند، و بسته‌ها امضای GPG ندارند.

### ۱۵. هیچ telemetry‌ای، همیشه
پروژه نباید هیچ‌وقت متن یا تصویر کلیپ‌بورد را جایی بفرستد. اگر روزی crash report
اضافه شد، باید opt-in باشد و هیچ محتوایی نفرستد.
