# تغییرات

## 0.8.0

این نسخه از یک بررسی بیرونی روی نسخهٔ ۰.۷.۰ آمد. هر مورد اول با کد واقعی تطبیق داده
شد و سپس رفع؛ مواردی که از قبل درست بودند هم همان‌جا علامت خوردند.

### رفع اشکال

- **`--force` در `clipnest export` باعث هشدار دروغ می‌شد.** لیست flagهای معتبر فقط
  `--json` و `--raw` بود، در حالی که `--force` کار می‌کرد؛ پس
  `clipnest export ~/backup --force` هم فایل‌ها را می‌نوشت و هم
  «ignoring unknown option(s): --force» چاپ می‌کرد. حالا فهرست flagها یک منبع واحد است
  و تست می‌کند که هر گزینهٔ `clipnest help` در parser هم شناخته می‌شود.
- **گزینهٔ ناشناس و مقدار گم‌شده حالا خطاست، نه هشدار.** `clipnest list --limt 5`
  قبلاً هشدار می‌داد و بعد **کل تاریخچه** را چاپ می‌کرد؛ `--limit --json` هم `--json`
  را به‌عنوان عدد می‌خواند، و `--out` بدون مقدار یک مسیر خالی می‌ساخت. هر سه حالا
  با پیام صریح و کد خروج ۲ رد می‌شوند. `--` هم پشتیبانی می‌شود تا جست‌وجوی رشته‌ای که
  با خط تیره شروع می‌شود ممکن باشد.
- **توکن اجازهٔ پیست با permission پیش‌فرض (۰۶۴۴) نوشته می‌شد.** این فایل در واقع
  اجازهٔ تایپ‌کردن در سشن کاربر است؛ حالا با mode `0600` و پوشه‌اش با `0700` نوشته
  می‌شود، permission روی فایل موقت و **قبل از** rename ست می‌شود (تا پنجرهٔ بازی
  باقی نماند)، و خطای نوشتن دیگر بی‌صدا نیست: `clipnest` در لاگ می‌گوید که اجازه را
  به یاد نمی‌آورد و در نتیجه دیالوگ دوباره پرسیده می‌شود.
- **مهاجرت دیتابیس تراکنشی شد و دیگر نمی‌تواند تاریخچه را ببرد.** مهاجرت قبلاً چند
  دستور جدا بود: اگر بین `ALTER TABLE items RENAME TO items_legacy` و کپی‌برگرداندن
  ردیف‌ها قطع می‌شد، اجرای بعدی جدول `items` را نمی‌دید، خالی می‌ساخت و داده با نام
  قدیمی رها می‌شد. حالا همهٔ مراحل داخل یک تراکنش‌اند، جدول رهاشده (از نسخه‌های قبلی)
  در اجرای بعدی بازیابی می‌شود، جدولی که این نسخه نمی‌تواند بخواند پاک نمی‌شود، و
  **قبل از هر تغییری یک کپی** با `VACUUM INTO` کنار فایل گذاشته می‌شود
  (`history.pre-v2-migration.db`).
- **import می‌توانست از بیرون پوشهٔ پشتیبان فایل بخواند.** `index.tsv` یک فایل متنی
  است و می‌توانست `../../.ssh/id_rsa` باشد؛ آن فایل خوانده و به‌عنوان آیتم کلیپ‌بورد
  ذخیره می‌شد (`clipnest get` هم چاپش می‌کرد). حالا فقط نام‌های بدون جداکننده پذیرفته
  می‌شوند، symlink رد می‌شود (هم در حالت index و هم در حالت خواندن پوشه)، و فایلی که
  از بزرگ‌ترین سقف تنظیمات بزرگ‌تر است **قبل از خواندن** رد می‌شود (قبلاً یک فایل ۴۰GB
  را در حافظه می‌خواند).
- **اکستنشن می‌توانست دو تصویر متفاوت را یکی ببیند و دومی را بی‌صدا دور بریزد.**
  fingerprint تصویر از طول + ابتدا + انتهای آن ساخته می‌شد، پس دو اسکرین‌شات که فقط
  در میانه (یک خط ترمینال، جای نشانگر) تفاوت داشتند، یکسان دیده می‌شدند. حالا
  تصویرهای کوچک‌تر از ۱MiB **کامل** هش می‌شوند و برای بزرگ‌ترها سه برش (ابتدا، میانه،
  انتها) هش می‌شود.
- **callbackهای اکستنشن بعد از disable روی objectهای null کار می‌کردند** و نتیجهٔ
  تنظیمات هم می‌توانست نصفه اعمال شود (مثلاً سقف تصویر خوانده می‌شد ولی سقف متن نه).
  حالا هر callback اول `_enabled` را چک می‌کند و تنظیمات یک‌جا و اتمیک اعمال می‌شوند.
- **`doctor` می‌گفت «system package» حتی وقتی کپی کاربری در حال اجرا بود.** روی
  دسکتاپ معمولی `~/.local/bin` جلوتر از `/usr/bin` است، پس یک کپی "کاربری" از
  `make install` اجرا می‌شد در حالی که بسته سالم به نظر می‌رسید و `doctor` هم
  خطای `layout: system package` را چاپ می‌کرد — یعنی همان خطی که باید بگوید چه
  چیزی اجرا می‌شود، دروغ می‌گفت. حالا هر دو حالت تشخیص داده می‌شود: اگر کپی
  کاربری در حال اجرا باشد، هشدار می‌دهد که کدام‌یک اجرا می‌شود و
  `make uninstall` را پیشنهاد می‌کند؛ اگر باینری بسته در حال اجرا باشد و کپی
  کاربری وجود داشته باشد، همان را نام می‌برد. (منطقش به یک تابع خالص با تست
  تبدیل شد.)
- **`doctor` سشن را از روی اسم دسکتاپ قضاوت می‌کرد.** روی همین سیستم، اوبونتو ۲۶.۰۴
  `XDG_CURRENT_DESKTOP=Unity` می‌گذارد در حالی که GNOME Shell 50.1 در حال اجراست؛
  تشخیص مبتنی بر اسم، برای دقیقاً همان کاربر هدف می‌گفت «دسکتاپ شما پشتیبانی نمی‌شود».
  حالا ملاک، وجود gnome-shell و وضعیت واقعی اکستنشن است و نسخهٔ shell هم گزارش می‌شود.

### قابلیت‌های جدید

- **`clipnest paste-reset`** اجازهٔ پیست را برمی‌گرداند (توکن را پاک می‌کند). عمداً
  جدا از تاریخچه: فایل دیگری است، تست هم دارد که پاک‌کردن اجازه تاریخچه را نمی‌برد.
- **`make bootstrap`** پیش‌نیازهای ساخت را *بررسی* می‌کند و می‌گوید چه کم است؛ چیزی
  نصب نمی‌کند (تغییر وضعیت package manager تصمیم کاربر است).
- **`make test-extension`** و **`packaging/tests/install-smoke.sh`**: اولی ۳۲ تست منطق
  اکستنشن را با `gjs` (یا `node`) اجرا می‌کند، دومی متادیتای بسته، فهرست فایل‌ها،
  یکسان‌بودن فایل‌های اکستنشن داخل بسته با مخزن و نصب واقعی با `apt` را بررسی می‌کند.
- **CI روی Ubuntu 26.04**: یک job روی رانر `ubuntu-26.04` کامپایل و تست می‌کند و یک job
  بستهٔ ساخته‌شده روی ۲۴.۰۴ را روی ۲۶.۰۴ **نصب و اجرا** می‌کند. job مربوط به
  `cargo fmt` هم اضافه شد ولی advisory است، چون پروژه روی ماشینی توسعه یافته که
  rustfmt نداشت.

### تغییرات

- منطق تصمیم‌گیری اکستنشن به `extension/lib/clipboard.js` منتقل شد (بدون هیچ import از
  GNOME) تا قابل تست باشد؛ `extension.js` فقط چیزهایی را نگه داشت که به gnome-shell
  نیاز دارند. بسته، تارْبال و `setup` هم این فایل را نصب می‌کنند.
- شمارهٔ نسخهٔ اکستنشن به **۵** رسید (`metadata.json`). این شماره همان چیزی است که
  `doctor` با آن می‌فهمد gnome-shell هنوز فایل‌های قدیمی را در حافظه دارد: تا وقتی
  logout/login نشود، نسخهٔ داخل سشن قدیمی است و `doctor` همین را می‌گوید. بدون این
  تغییر، دو نسخهٔ متفاوت هر دو «۴» بودند و هشدار داده نمی‌شد.
- `Row::json` و escape آن دست‌نخورده ماند (درست بود)، ولی حالا تست با یک **parser واقعی
  JSON** (`serde_json` فقط به‌عنوان dev-dependency) بررسی می‌شود: کوتیشن، backslash،
  newline، tab، کاراکتر کنترلی، فارسی و emoji.
- README دیگر نسخهٔ ثابت در دستور نصب ندارد؛ به صفحهٔ آخرین انتشار لینک می‌دهد، دربارهٔ
  بررسی `SHA256SUMS` می‌گوید و جدول سازگاری را با «چه چیزی واقعاً تست شده» بازنویسی
  می‌کند. `ROADMAP.md` هم کارهای باقی‌مانده را با اولویت ثبت می‌کند.

## 0.7.0

### قابلیت‌های جدید

- **پیست خودکار.** حالا انتخاب یک آیتم فقط کپی نمی‌کند، بلکه **همان آیتم در پنجره‌ای که
  قبلاً در آن بودی پیست هم می‌شود** — دقیقاً رفتار `Win+V` ویندوز. مسیرش
  `org.freedesktop.portal.RemoteDesktop` است: دیمن یک سشن از Portal می‌گیرد
  (`CreateSession` → `SelectDevices` با `types=keyboard` → `Start`) و بعد با
  `NotifyKeyboardKeysym` کلید‌های میانبر را می‌فرستد. هیچ ابزار بیرونی (`ydotool`،
  `wtype`) و هیچ دسترسی root لازم نیست، و پنل قبل از فرستادن کلیدها منتظر می‌ماند تا
  فوکوس واقعاً به پنجرهٔ قبلی برگردد (هر ۳۰ms بررسی می‌شود، با سقف ۷۵۰ms).
  - **یک بار اجازه می‌خواهد.** GNOME اول یک دیالوگ نشان می‌دهد؛ `clipnest paste-access`
    همین را از قبل می‌پرسد، `clipnest setup` هم بعد از نصب خودش صدایش می‌زند، و
    توکن برگشتی در `~/.local/share/clipnest/portal-restore-token` نگه داشته می‌شود
    (`persist_mode`) تا سؤال تکرار نشود. اگر بک‌اند `persist_mode` را قبول نکند،
    بدون آن دوباره تلاش می‌شود و سؤال هر سشن پرسیده می‌شود — نه خطا.
  - **دلیلِ شکست، در همان لحظهٔ شکست ثبت می‌شود، نه در لحظهٔ خواندن پیام.** چهار
    حالتی که کاربر باید بتواند از هم تشخیص بدهد حالا جدا هستند و به‌صورت
    `state` + `reason` روی D-Bus می‌روند: `asking` (منتظر دیالوگ)، `ready` (اجازه
    گرفته و یک کلید آزمایشی هم واقعاً رسیده)، `denied` (کاربر لغو کرد)، `locked`
    (صفحه قفل است)، `unavailable` (Portal نیست یا بک‌اندش RemoteDesktop ندارد) و
    `broken` (سشن گرفته شد ولی تایپ کردن شکست خورد). قبلاً همهٔ این‌ها یک `failed`
    بودند و CLI و `doctor` با پرسیدن `org.gnome.ScreenSaver.GetActive` **در لحظهٔ
    گزارش** حدس می‌زدند کدام است — یعنی اگر کاربر تا خواندن پیام صفحه را باز کرده
    بود، پیام دروغ می‌گفت. حالا هر حالت دستور بعدی خودش را چاپ می‌کند
    (`clipnest paste-access`، «صفحه را آنلاک کن»، یا «`xdg-desktop-portal-gnome`
    را نصب کن»)، و `doctor` هم همین خط را نشان می‌دهد.
  - **`unavailable` از `broken` جدا شد.** روی سشنی که تنها بک‌اندش `portal-gtk`
    بود اندازه‌گیری شد: Portal پاسخ `UnknownMethod: No such interface
    org.freedesktop.portal.RemoteDesktop` می‌دهد. این «سشن خراب» نیست — چیزی برای
    تعمیر وجود ندارد و راه‌حل یک بسته است — پس پیام صریحاً
    `xdg-desktop-portal-gnome` (یا بک‌اند دسکتاپ کاربر) را نام می‌برد و
    `systemctl restart` پیشنهاد نمی‌کند.
  - **`clipnest paste <n>` دیگر دروغ نمی‌گوید.** تا وقتی اجازه گرفته نشده باشد،
    خروجی «copied; …» است با همان دلیل و راه‌حل، نه «pasting with <ctrl>v».
  - **صفحهٔ قفل.** GNOME تا وقتی lock screen بالاست ساخت سشن RemoteDesktop را رد
    می‌کند و آن را یا با کد ۲ و یا با خطای «Session creation inhibited» می‌گوید.
    هر دو مسیر حالا به `locked` طبقه‌بندی می‌شوند (و فقط در همین حالت از پوسته
    پرسیده می‌شود)، و هیچ چیز از دست نمی‌رود: آیتم روی کلیپ‌بورد هست و تلاش بعدی
    دوباره سشن می‌سازد.
- **`clipnest paste <n>`** همان کار را از خط فرمان می‌کند (کپی + پیست در پنجرهٔ فعال)،
  و `clipnest pick <n>` مثل قبل **فقط کپی** می‌کند. این تفکیک عمدی است: یک دستور که
  ناخواسته در پنجرهٔ فعال تایپ کند غافلگیرکننده است، پس پیست باید صریح خواسته شود.
- **`paste_key` در فایل تنظیمات** (پیش‌فرض `<Control>v`). پیست خودکار اولین تنظیمی است
  که مقدارش عدد نیست: با همان دستور زبان میانبرهای GNOME اعتبارسنجی می‌شود
  (`Control+v` بنویسی، پیام می‌گوید `'<Control>v'`؛ `<Mod4>v` بنویسی، می‌گوید GDK آن را
  `<Super>` می‌نامد)، و `paste_key = none` پیست را خاموش می‌کند و فقط کپی می‌ماند.
- **بسته‌سازی چند-توزیعی:** `make dist` یک پوشهٔ `dist/` مرتب می‌سازد — بستهٔ `.deb`
  برای amd64، تارْبال قابل‌حمل (هر توزیعی با همان معماری)، تارْبال سورس، `PKGBUILD`
  آرچ، اسپک RPM، و `SHA256SUMS`. ورک‌فلوی `release.yml` هم همان مجموعه را روی هر
  توزیع/معماری از خودِ گیت‌هاب می‌سازد (amd64 و arm64) و به انتشار می‌چسباند.
- **`RELEASING.md`:** از `git init` تا تگ و انتشار؛ دقیقاً همان دستورهایی که لازم است.

### تمیزکاری و ریز‌ایرادها

- **کَش بندانگشتی پنل:** درج دوبارهٔ یک آیتم با اندازهٔ بزرگ‌تر، نسخهٔ قبلی‌اش را در
  بودجه رها نمی‌کند (قبلاً یک ورودی مرده بودجه را اشغال می‌کرد).
- **`Config` دیگر `Copy` نیست** و `paste` در `paste.rs` جدا شده‌است؛ `app.rs` فقط دیمن و
  پنل است، `install.rs` فقط نصب، و پروتکل Portal در یک ماژول تست‌شده جمع است.
- **`Suggests` در بستهٔ deb** برای `xdg-desktop-portal` و بک‌اند گنوم: نصب کمینه‌ای که
  پورتال ندارد حالا می‌فهمد چه چیزی کم است، بدون این‌که وابستگی اجباری شود.
- **`doctor` یک خط تازه دارد** برای وضعیت پیست خودکار (آماده / اجازه لازم دارد /
  خاموش) و دلیلِ واقعیِ شکست را از دیمن می‌خواند، نه از وضعیت فعلی صفحه.
- **طبقه‌بندی دلیل، تست واحد دارد.** `classify_refusal` و `classify_call_error`
  بررسی می‌شوند: «قفل + کد ۲» → `locked`، «unable to ask» → نه `locked`،
  «Not provided by any .service files» و «No such interface …RemoteDesktop» →
  `unavailable`، و واژه‌های قفل با صفحهٔ باز → `broken`. تست زندهٔ Portal هم یک
  کلید بی‌اثر (`Shift`) می‌فرستد تا «اجازه گرفته شد» و «واقعاً تایپ می‌شود» دو
  ادعای جدا باشند.
- **بستهٔ RPM دیگر یونیت اشتباه نصب نمی‌کند:** اسپک `data/clipnest.service`
  (نسخهٔ کاربری، `ExecStart=%h/.local/bin/clipnest`) را در
  `/usr/lib/systemd/user/` می‌گذاشت، یعنی روی نصب سیستمی دیمن هرگز بالا نمی‌آمد.
  حالا نسخهٔ `data/deb/` نصب می‌شود و تارْبال هر دو را همراه دارد.
- **ورک‌فلوی انتشار برای RPM هرگز موفق نمی‌شد:** نام نسخه در اسپک `@VERSION@` است و
  `rpmbuild` نمی‌تواند ماکرویی را که وجود ندارد در تگ `Version` باز کند؛
  ورک‌فلو با `--define "version …"` سعی می‌کرد و بستهٔ نامعتبر می‌ساخت. حالا مثل
  `make dist-extra` جای‌گذاری می‌شود و همان فایلِ dist ساخته می‌شود.
- **پوشهٔ `.deb` به نام کفِ پشتیبانی است، نه نام میزبان ساخت:** کف از خود بسته
  خوانده می‌شود (`libc6 (>= 2.39)` → `dist/ubuntu-24.04-amd64/`)، و `dist/` یک
  پوشهٔ `ubuntu-22.04-amd64/` هم دارد که می‌گوید چرا چنین بسته‌ای وجود ندارد
  (GNOME 42 و GTK 4.6 در برابر GTK 4.8 و Shell 45 لازم) — و اگر روزی واقعاً ساخته
  شد، خودِ Makefile آن توضیح را نمی‌نویسد.
- **`make dist` هشدار می‌دهد** اگر `GH` هنوز `USERNAME` باشد، تا بسته‌ای با آدرس
  گیت‌هاب ناموجود منتشر نشود (`make set-github GH=<account>`).
- **`make preflight`** پیش از اولین push همان چیزهایی را می‌گیرد که از داخل کد
  دیده نمی‌شوند و موقع انتشار شبیه «پروژه خراب است» به نظر می‌رسند: نبودِ هویت git
  (git اصلاً کامیت نمی‌کند)، `USERNAME` باقی‌مانده در متادیتا، و `dist/`ی که پیش از
  آخرین ویرایش ساخته شده (سورس تازه تگ می‌خورد ولی باینری قدیمی منتشر می‌شود). هر چه
  لازم است چاپ می‌کند و دستورهای واقعی push را با نسخهٔ درست نشان می‌دهد؛ هیچ‌چیز را
  عوض نمی‌کند.
- **`.gitignore` حالا `/.freebuff` را هم رد می‌کند** (وضعیت داخلی ابزار است، نه
  بخشی از پروژه) و `/dist` بیرون می‌ماند: بستهٔ نصب جای‌اش تب Releases است، نه درخت
  مخزن، تا هر نسخه یک کپی ۲۸۸ کیلوبایتی دیگر در تاریخچه نگذارد.
- **تست زندهٔ Portal** که فقط با `--ignored` اجرا می‌شود: نیمهٔ اول دست‌دادن
  (`CreateSession` + `SelectDevices`) را روی سشن واقعی جواب می‌دهد و `Start` را صدا
  نمی‌زند، چون آن یکی دیالوگ باز می‌کند و جای تست خودکار نیست.

## 0.6.0

### قابلیت‌های جدید

- **تنظیمات از فایل.** `~/.config/clipnest/config.ini` و دستور `clipnest config` برای
  دیدن مقادیر مؤثر: `max_items`، `image_budget_mb`، `max_text_mb`، `max_image_mb`،
  `panel_limit`، `poll_interval_ms`، `max_age_days`. دیگر برای عوض‌کردن یک سقف نیازی به
  ویرایش سورس و کامپایل مجدد نیست. سقف‌ها و بازهٔ poll را **اکستنشن هم از دیمن
  می‌پرسد** (متد `Config` روی D-Bus): اگر نمی‌پرسید، بالا بردن `max_image_mb` بی‌اثر
  می‌ماند، چون اکستنشن تصویر بزرگ را زود‌تر رد می‌کرد و دیمن هرگز آن را نمی‌دید.
- **پشتیبان‌گیری و بازگردانی:** `clipnest export <dir>` و `clipnest import <dir>`. هر
  آیتم یک فایل است (متن `.txt`، تصویر با همان بایت‌های اصلی کلیپ‌بورد، بدون کدگذاری
  مجدد) و `index.tsv` می‌گوید کدام فایل کدام آیتم است و پین و زمان‌ها چه بودند.
  Import یک merge است: آیتم‌های موجود دست‌نخورده می‌مانند و ترتیب تاریخچه به‌هم
  نمی‌ریزد، پس دو بار import کردن یک پشتیبان یک بار اثر دارد. پوشهٔ بدون `index.tsv` هم
  (همهٔ فایل‌ها بر اساس نام و محتوا) قابل import است.
- **حذف، فضا را واقعاً آزاد می‌کند.** `delete` و `clear` بعد از پاک‌کردن ردیف‌ها
  صفحه‌های آزادشده را با `incremental_vacuum` به سیستم برمی‌گردانند و همان عدد را چاپ
  می‌کنند (`entry 42 deleted, freed 1.4 MiB`). `stats` مقدار `reclaimable` را هم
  نشان می‌دهد و `vacuum` علاوه بر `VACUUM` فایل `-wal` را هم جمع می‌کند. آیتم پین‌شده
  از هر سه قاعدهٔ هرس (تعداد، بودجهٔ حجم، سن) معاف است.
- **`clipnest setup`:** یک دستور که کارهای مربوط به حساب کاربر را تمام می‌کند — گذاشتن
  اکستنشن (اگر بسته گذاشته باشد فقط فعالش می‌کند)، `gnome-extensions enable`، ثبت
  میانبر، `systemctl --user daemon-reload` و ری‌استارت سرویس. `make install` هم همین را
  صدا می‌زند، پس هر دو نوع نصب به یک حالت می‌رسند.
- **بستهٔ `.deb`:** `make deb` و `make deb-verify` (بازرسی بسته بدون نصب). وابستگی‌ها با
  `dpkg-shlibdeps` از خودِ باینری خوانده می‌شوند، پس بسته روی هر توزیعی که همان
  کتابخانه‌ها را دارد می‌نشیند. ورک‌فلوی گیت‌هاب با تگ `v*` همان بسته را می‌سازد و به
  انتشار می‌چسباند.
- `.desktop` و آیکون برای شناسهٔ برنامهٔ پنل (`dev.clipnest.Panel`)، پس پنل در شل نام و
  آیکون دارد و پنجرهٔ بی‌نام نیست.

### رفع باگ

- **توصیهٔ خطرناک حذف نسخهٔ سیستمی.** `install-extension` (و README) برای هر کپی سیستمی
  `sudo rm -rf /usr/share/gnome-shell/extensions/clipnest@clipnest.dev` چاپ می‌کرد. بعد از
  نصب بسته، همان مسیر **خودِ بسته** است — یعنی کاربر با دنبال‌کردن راهنمای خود ابزار،
  فایل‌های بسته را پاک می‌کرد. حالا کپی سیستمی که با این نسخه یکی است به‌عنوان «بسته‌شده»
  گزارش می‌شود، تفاوت‌ها بدون هیچ پیشنهاد مخربی هشدار می‌گیرند، و `doctor` می‌گوید کدام
  نسخه بار می‌شود (کاربری یا بسته‌ای).
- **`doctor` و مسیرها فقط نسخهٔ کاربری را می‌شناختند.** یونیت systemd، فایل فعال‌سازی
  D-Bus و اکستنشن در مسیرهای سیستمی ❌ می‌گرفتند. حالا هر دو مسیر بررسی می‌شود، `layout`
  گزارش می‌شود (`/usr` یا `~/.local`)، و اگر مدیر کاربر یونیت تازه را ندیده باشد
  (`systemctl --user show -p FragmentPath`) پیام درست `daemon-reload` است.
  سایه‌انداختن `~/.local/bin/clipnest` روی نسخهٔ بسته‌ای هم با ⚠️ گزارش می‌شود.
- **`PRAGMA incremental_vacuum;` عملاً یک صفحه آزاد می‌کرد، نه همه.** `execute_batch`
  فقط **یک** گام از استیتمنت را برمی‌دارد و یک گام این pragma یک صفحه را جابه‌جا می‌کند
  (اندازه‌گیری شد: از ۱۱۷۶ صفحهٔ آزادشده، ۱۱۷۵ صفحه سرِ جایش می‌ماند). حالا استیتمنت تا
  پایان گام‌به‌گام اجرا می‌شود و فایل واقعاً کوچک می‌شود.
- **`LIKE … COLLATE NOCASE` تشریفاتی بود.** `LIKE` در SQLite به‌صورت پیش‌فرض برای ASCII
  حساس به بزرگی/کوچکی نیست، و آن `COLLATE` روی نتیجهٔ بولین اعمال می‌شد نه روی مقایسه.
- **`clipnest list --limit 0`** بی‌صدا هیچ چاپ می‌کرد. حالا `--limit` اعتبارسنجی می‌شود و
  مقدار نامعتبر (یا `--limt` تایپی) پیام روشن می‌دهد.
- **`clipnest search hello world`** کلمهٔ دوم را دور می‌ریخت؛ حالا همهٔ آرگومان‌های
  موقعیتی بخشی از عبارت جستجو هستند.
- **هر کپی، پنلِ بسته را هم بازسازی می‌کرد.** بعد از اولین باز شدن پنل، هر تغییر
  کلیپ‌بورد تا ۳۰۰ ردیف ویجت می‌ساخت، حتی وقتی پنل بسته بود. حالا پنل «کثیف» علامت
  می‌خورد و در `show` بعدی بازسازی می‌شود.
- **کش بندانگشتی‌ها سقف تعدادی داشت، نه حجمی.** تصویر ۴K دیکد‌شده حدود ۴۸MiB است، پس
  چهل تا یعنی نزدیک ۲ گیگابایت RAM. حالا سقف بایتی است (۹۶MiB) و قدیمی‌ترین دیکد اول
  بیرون می‌رود.
- **`import` می‌توانست دو کپیِ یک میلی‌ثانیه را جابه‌جا کند.** ترتیب دو آیتم هم‌زمان بر
  اساس `id DESC` تعیین می‌شود و `import` آیدی‌ها را از نو می‌داد؛ حالا آیدی اصلی نگه داشته
  می‌شود (اگر آزاد باشد) و ترتیب یک پشتیبانِ برگردانده‌شده همان ترتیب قبلی است.
- **`bundled_extension_version()`** فرض می‌کرد فایل `[0]` همان `metadata.json` است؛ حالا با
  نام پیدا می‌شود.
- **`clipnest stats` بدون دیمن کار می‌کرد:** حالا اگر دیمن بالا نباشد اعداد را خودش از
  فایل می‌خواند (مثل `list` و `get`).

### تغییرات شکستن‌پذیر

- CLI و دیمن باید با هم به‌روز شوند: متدهای تازهٔ `Config`، `Refresh` و `DiskUsage` روی
  D-Bus و پاسخ‌های `Delete`/`Clear` که حالا بایت آزادشده را هم برمی‌گردانند.
- `extension.js` عوض شده (خواندن تنظیمات از دیمن) و نسخهٔ اکستنشن ۴ است، پس این ارتقا
  **یک‌بار logout/login لازم دارد** — برخلاف ۰.۵. اگر فراموش کنی، `doctor` می‌گوید
  نسخهٔ در حال اجرا قدیمی است و راه‌حلش را می‌دهد.

### تست

- ۴۹ تست (قبلاً ۱۷): پارسر تنظیمات (کامنت، کلید ناشناس، مقدار نامعتبر، نمونهٔ پیشنهادی،
  صفر = خاموش)، اعمال محدودیت‌ها در هرس، محدودیت سن و معاف‌بودن پین‌شده‌ها، بودجهٔ صفر
  تصویر، آزادسازی فضا روی دیتابیس فایل‌دار، `restore` بدون به‌هم‌ریختن ترتیب و بدون
  تکرار، رفت‌وبرگشت `export`/`import`، پشتیبان آسیب‌دار، پوشهٔ بدون index، سیاست
  eviction کش بندانگشتی، مسیرهای هر دو نوع نصب، و هم‌خوانی فایل‌های بسته (یونیت کاربری
  و بسته‌ای، فایل‌های D-Bus) با کد.

## 0.5.0

### قابلیت‌های جدید

- **میانبر قابل تنظیم است.** `clipnest setup-shortcut --binding '<Super><Shift>c'` و
  سر نصب `make install BINDING='<Super>c'`. پیش‌فرض همان `Super+Shift+V` می‌ماند.
  اعتبارسنجی میانبر دستی است، چون `gtk::accelerator_parse` بدون `gtk::init` پنیک
  می‌کند و `setup-shortcut` را روی SSH (بدون نمایشگر) می‌شکست. قواعدش با خود GDK 4
  تطبیق داده شده: `<Win>v`، `<Mod4>v` و `<Super>vg` همان‌جا رد می‌شوند، چون GDK هم
  ردشان می‌کند - پذیرفتن‌شان یعنی ثبت میانبری که کار نمی‌کند. برای اشتباه رایج
  (نوشتن `Super+v` یا `Win+v` بدون براکت) پیام دقیقاً شکل درست را می‌گوید.
- **هشدار تعارض میانبر.** `setup-shortcut` و `doctor` میانبرهای خود گنوم
  (`org.gnome.desktop.wm.keybindings`، `org.gnome.shell.keybindings`، media-keys) را
  می‌گردند و اگر چیزی روی همان ترکیب نشسته باشد گزارش می‌دهند - مثلاً `<Super>v` که
  `toggle-message-tray` است. گنوم خودش چنین تعارضی را هیچ‌وقت اعلام نمی‌کند.
- **جستجو در CLI:** `clipnest search <query>` و `clipnest list --search <query>`، روی
  متد تازهٔ `Search(s needle, u limit)` در D-Bus. `List(u)` برای CLIهای قدیمی‌تر دست
  نخورده باقی می‌ماند.

### رفع باگ

- **`doctor` میانبر سفارشی را خراب اعلام می‌کرد.** مقایسه با `SHORTCUT_BINDING` سخت‌کد
  بود، پس هر میانبری که کاربر در تنظیمات گنوم عوض می‌کرد ❌ می‌گرفت، در حالی که کار
  می‌کرد. حالا ثبت‌شدن مسیر، درست بودن دستور و معتبر بودن خودِ binding بررسی می‌شود و
  مقدار سفارشی فقط چاپ می‌شود (نه خطا).
- **`Tab` فوکوس را از باکس جستجو می‌برد** (ردیف‌های `GtkListBoxRow` فوکوس‌پذیرند) و از
  آن لحظه هرچه تایپ می‌کردی بی‌صدا دور ریخته می‌شد. حالا `Tab`/`Shift+Tab` انتخاب را
  جابه‌جا می‌کنند و فوکوس در باکس جستجو می‌ماند.
- **`%` و `_` در جستجو وایلدکارد `LIKE` بودند.** `clipnest search 100%` (و همان باکس
  پنل) هر چیزی را برمی‌گرداند. حالا با `ESCAPE` فرار داده می‌شوند.
- **`database is locked`** وقتی CLI همزمان با نوشتن دیمن می‌خواند: هر دو اتصال حالا
  `busy_timeout=5000` دارند.
- **باز کردن دیتابیس WAL به‌صورت فقط‌خواندنی** وقتی `-shm` وجود ندارد شکست می‌خورد و
  به مسیر نوشتنی می‌افتاد که migration اجرا می‌کرد. حالا اتصال عادی با `query_only=ON`
  باز می‌شود؛ فایل غایب همچنان خطای اصلی را برمی‌گرداند تا caller دیتابیس تازه بسازد.
- **گزینهٔ اشتباه بی‌صدا نادیده گرفته می‌شد:** `clipnest list --limt 5` بدون هیچ پیامی
  کل تاریخچه را چاپ می‌کرد. حالا هشدار می‌دهد.
- آیتم انتخاب‌شده روی `PRIMARY` هم گذاشته می‌شود، پس paste با دکمهٔ وسط موس همان آیتمی
  را می‌دهد که انتخاب کرده‌ای، نه انتخاب قبلی.

### تست

- ۱۷ تست (قبلاً ۵): سازگاری ثابت‌های `extension.js` با `db.rs` (تا سقف اندازه‌ها
  بی‌سروصدا از هم دور نشوند)، اعتبارسنجی/نرمال‌سازی میانبر و متن خطاهایش، داوری میانبر
  در `doctor`، خواندن گزینه‌های CLI و فرار کاراکترهای `LIKE`.

`extension.js` در این نسخه عوض نشده، پس این ارتقا **logout/login لازم ندارد**: فقط
`make install` کافی است.

## 0.4.0

### تغییر معماری راه‌اندازی

- **دیمن دیگر در لاگین اجرا نمی‌شود؛ با D-Bus فعال می‌شود.** یونیت به `Type=dbus`
  با `BusName=dev.clipnest.Daemon` تغییر کرد و `WantedBy=graphical-session.target`
  حذف شد. اولین تماس (کپی از طرف اکستنشن، یا `clipnest toggle`) دیمن را بالا می‌آورد.
  دلیلش یک باگ واقعی است: اجرا در لاگین باعث می‌شد مقداردهی GTK پیش از آماده شدن
  شل به `xdg-desktop-portal` دست بزند، بک‌اند GTK آن با `cannot open display`
  بمیرد و از آن لحظه هر برنامه‌ای که از Portal چیزی بخواهد پشت تایم‌اوت ۲۵
  ثانیه‌ای D-Bus بماند — کروم، ناوتیلوس و فایل‌رولر دقیقه‌ها باز نمی‌شدند. حالا
  کسی که دیمن را صدا می‌زند خودِ شل است (تنها منبع دادهٔ کلیپ‌بورد)، پس این ترتیب
  دیگر قابل نقض نیست.
- **پنل تنبل ساخته می‌شود.** استور و D-Bus از پنجره جدا شدند و پنجره فقط در اولین
  `Toggle` ساخته می‌شود؛ در نتیجه `clipnest list`/`status`/`pick` هیچ‌وقت پنجره
  نمی‌سازند و دیمن بدون نمایشگر هم زنده می‌ماند (قبلاً `exit(1)` می‌کرد).
  `app.hold()` هم اضافه شد تا GTK با نبود پنجره پروسه را نبندد.
- **نصب:** `make install` دیگر `systemctl --user enable clipnest` را صدا نمی‌زند
  (یونیت بخش `[Install]` ندارد) و به‌جایش enable قدیمی را پاک می‌کند. برای
  مهاجرت فقط یک‌بار `make install` لازم است.

### محافظ اختیاری Portal

- هدف جدید `make install-portal-guard` دو drop-in برای
  `xdg-desktop-portal.service` و `xdg-desktop-portal-gtk.service` می‌نویسد
  (`Restart=on-failure`، `TimeoutStartSec=30`) تا اگر روزی برنامهٔ دیگری هم
  Portal را در لاگین خراب کرد، سشن بعد از چند ثانیه ری‌استارت شود نه چند دقیقه
  معلق بماند. با `make uninstall-portal-guard` کامل برمی‌گردد.

## 0.3.0

### رفع باگ‌های بحرانی

- **متن هیچ‌وقت ثبت نمی‌شد.** کد داخل کال‌بک `get_text_finish` را صدا می‌زد؛ چنین متدی
  در GJS وجود ندارد (`St.Clipboard.get_text` فقط کال‌بکی است) و استثنا در `try/catch`
  بی‌صدا خورده می‌شد. حالا `get_text(type, callback)` مستقیم استفاده می‌شود.
- **اولین کپی تصویر کل ثبت تاریخچه را می‌کشت.** `get_content` با امضای اشتباه
  (`type, callback` به‌جای `type, mimetype, callback`) صدا زده می‌شد، استثنا بیرون
  می‌پرد و پرچم `_reading` تا پایان سشن `true` می‌ماند. حالا امضا درست است و یک
  نگهبان ۳ ثانیه‌ای `_reading` را آزاد می‌کند.
- **`Backspace` آیتم تاریخچه را پاک می‌کرد** در حالی که کاربر در باکس جستجو تایپ
  می‌کرد. حالا فقط وقتی باکس جستجو فوکوس ندارد حذف می‌کند و حذف اصلی با `Delete` است.
- **بعد از اولین `↑`/`↓` تایپ از کار می‌افتاد**، چون فوکوس از باکس جستجو به ردیف
  منتقل می‌شد. حالا انتخاب بدون گرفتن فوکوس انجام می‌شود و ردیف انتخابی دستی داخل
  دید اسکرول می‌شود.
- **ترتیب نامطمئن دو کپی در یک ثانیه.** زمان‌ها میلی‌ثانیه‌ای شدند و مرتب‌سازی یک
  tie-breaker (`id DESC`) گرفت؛ نسخه‌های قدیمی هنگام مهاجرت به‌صورت خودکار بزرگ می‌شوند.
- **سرویس بعد از logout/login دیگر بالا نمی‌آمد** (`WantedBy=default.target` و
  `PartOf=graphical-session.target`). حالا به `graphical-session.target` گره خورده و
  `Restart=always` است.

### تغییرات شکستن‌پذیر

- **اسکیمای دیتابیس نسخه‌دار شد (v2)** و مهاجرت خودکار انجام می‌شود. جدول قدیمی
  `clipboard_items` و جدول `items` با ستون `pixels` منتقل و حذف می‌شوند؛ ردیف‌های
  تصویری خام کنار گذاشته می‌شوند چون مسیر تولیدشان هیچ‌وقت قابل دسترس نبود.
- **ذخیرهٔ تصویر:** به‌جای پیکسل خام RGBA، همان کدگذاری کلیپ‌بورد (PNG/JPEG/…) ذخیره
  می‌شود؛ حجم ده‌ها برابر کمتر و رنگ‌ها درست.
- **رابط D-Bus عوض شد:** `PushImage(s mime, ay data)`، `List(u) → a(ussuub)`،
  `GetItem`، `Pick`، `SetPin`، `Delete`، `Clear`، `Vacuum`، `Status`. باید دیمن و CLI
  با هم به‌روز شوند (`make install`).

### قابلیت‌های جدید

- پین‌کردن آیتم‌ها (`Ctrl+P`، دکمهٔ پین روی ردیف، منوی راست‌کلیک) — آیتم پین‌شده هرگز
  با هرس پاک نمی‌شود و بالای لیست می‌ماند.
- CLI کامل: `get`، `pick`، `pin`/`unpin`، `delete`، `clear`، `stats`، `vacuum`،
  `doctor`، `remove-shortcut`، و `list --json --limit`.
- `clipnest doctor` تمام نصب را چک می‌کند: دیمن و نسخه‌اش، اسکیمای دیتابیس، هم‌خوانی
  فایل‌های اکستنشن با باینری، فعال بودن اکستنشن، میانبر، و سرویس.
- سقف حجم: بودجهٔ ۱۲۸MiB برای تصاویر غیرپین‌شده، سقف ۸MiB برای یک تصویر و ۱MiB برای
  یک متن، به‌همراه `auto_vacuum` تدریجی و دستور `vacuum`.
- پنل: حالت خالی، زمان نسبی و راهنمای فرمت تصویر، `Home/End/PageUp/PageDown`،
  منوی راست‌کلیک، و دیکد تنبل بندانگشتی‌ها (فقط ردیف‌های نزدیک به دید).
- `make uninstall` حالا میانبر و اکستنشن را هم پاک می‌کند؛ `make check` اضافه شد.

## 0.2.0

- نسخهٔ اول: دیمن Rust با پنل GTK4/libadwaita، اکستنشن gnome-shell، تاریخچهٔ SQLite،
  ثبت میانبر و نصب اکستنشن. (ثبت کلیپ‌بورد در این نسخه عملاً کار نمی‌کرد — بالا را
  ببین.)
